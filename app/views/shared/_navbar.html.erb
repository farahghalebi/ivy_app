<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
  <div class="container-fluid">
    <!-- Brand -->
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
      <img src="<%= asset_path('logo.png') %>" alt="Ivy Parfumerie" class="brand-logo me-2">
      <span class="fw-semibold">Scent Magpie</span>
    <% end %>

    <!-- Toggler -->
    <button class="navbar-toggler" type="button"
            data-bs-toggle="collapse" data-bs-target="#mainNav"
            aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapse -->
    <div class="collapse navbar-collapse" id="mainNav">
      <!-- Left: primary nav -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link" href="<%= root_path %>">Home</a></li>
        <li class="nav-item"><a class="nav-link" href="<%= perfumes_path %>">Perfumes</a></li>

        <!-- Create new perfume as a purple button -->
        <li class="nav-item ms-lg-2">
          <a class="btn btn-primary" href="<%= new_perfume_path %>">Create New Perfume</a>
        </li>
      </ul>

      <!-- Right: auth/profile -->
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center p-0" href="#"
               id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <img class="avatar me-2" alt="Profile"
                   src="<%= asset_path('logo.png') %>">
              <span class="d-none d-lg-inline"><%= current_user.try(:email) || "Profile" %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <!-- Devise settings page works as Profile -->
              <li><a class="dropdown-item" href="<%= edit_user_registration_path %>">Profile / Settings</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to "Log out", destroy_user_session_path,
                              data: { turbo_method: :delete }, class: "dropdown-item" %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item"><a class="nav-link" href="<%= new_user_session_path %>">Sign in</a></li>
          <li class="nav-item ms-2"><a class="btn btn-primary" href="<%= new_user_registration_path %>">Sign up</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
